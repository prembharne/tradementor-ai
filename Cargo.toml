[workspace]
members = [
    "contracts/strategy",
    "contracts/challenge",
    "contracts/reputation",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
overflow-checks = true

[workspace.dependencies]
soroban-sdk = { version = "20.0.0" }